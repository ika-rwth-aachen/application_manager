apiVersion: apps/v1
kind: Deployment
metadata:
  name: kopf-operator-object-fusion
  labels:
    tag: am
    app: kopf-operator-object-fusion
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kopf-operator-object-fusion
  template:
    metadata:
      labels:
        app: kopf-operator-object-fusion
    spec:
      terminationGracePeriodSeconds: 0
      nodeSelector:
        node_id: cloud
      serviceAccountName: service-account-kopf-operator
      containers:
        - name: kopf-operator-object-fusion
          image: k3d-application-manager-registry.localhost:41671/kopf_object_fusion_operator:latest
          imagePullPolicy: IfNotPresent
          command:
            - /bin/bash
            - -c
            - kopf run /operator/object_fusion_operator.py
          volumeMounts:
          - name: kopf-operator-config-volume
            mountPath: /config
          - name: kopf-operator-bookkeeping
            mountPath: /bookkeeping_data
      volumes:
      - name: kopf-operator-config-volume
        configMap:
          name: kubernetes-operator-configuration
      - name: kopf-operator-bookkeeping
        persistentVolumeClaim:
          claimName: object-fusion-operator-pvc