apiVersion: v1
kind: PersistentVolume
metadata:
  name: mqtt-operator-pv
  labels:
    tag: am
spec:
  storageClassName: standard
  capacity:
    storage: 10Mi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /bookkeeping/mqtt
---

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mqtt-operator-pvc
  labels:
    tag: am
spec:
  accessModes:
    - ReadWriteOnce
  volumeName: mqtt-operator-pv
  resources:
    requests:
      storage: 10Mi
  storageClassName: standard